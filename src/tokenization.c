
#include "../header/ft_minishell.h"
#include <readline/readline.h>
#include <readline/history.h>




int    ft_tokenization(char *input)
{
	if (!inputi || !*input)
		return ;
	if (ft_is_builtin(input, ))
		return ;
	if (ft_is_operator(input, ))
		return ;
	if (ft_check_var(input, ))
	
	ft_check_quotes();

	if (strncmp(input, "echo", ft_strlen("echo")))
	{
	}
	
    return ;
}

